// Configuration Management baseline for the as-is Base Architecture.
//
// Generated by /vse-setup brownfield as-is survey on {{DATE}}.
//
// Declares the BL-BA-AS-IS-0.1 baseline that every @ConfigItem
// application in {{PROJECT_NAME}}_BaseArchitecture references via
// its `baselineId` attribute. The scope listing names the part defs
// that the survey froze at this baseline.
//
// The baseline is the initial as-is reference. Subsequent baselines
// (BL-BA-AS-IS-0.2, BL-BA-AS-IS-0.3, ...) supersede this one and are
// declared by the user when a Discovery, Refinement, or Replacement
// event (§2.7) occurs. Each new baseline is reviewed under elevated
// final-review criteria per §2.6 rule 4 and §8.6.3.

package <{{PROJECT_SHORT_CODE}}_BA_CM> {{PROJECT_NAME}}_BaseArchitecture_CM {

    private import VSE_Library::Baseline;

    item def BL_BA_AsIs_01 {
        attribute id : String = "BL-BA-AS-IS-0.1";
        attribute date : String = "{{DATE}}";
        attribute description : String =
            "Initial as-is Base Architecture, captured at brownfield setup.";

        // {{AS_IS_MANDATED_SCOPE_LIST}}
        //
        // /vse-setup emits a comment block here listing the
        // fully-qualified names of the mandated part defs declared
        // in {{PROJECT_NAME}}_BaseArchitecture, one per line, for
        // example:
        //
        //     // scope:
        //     //   {{PROJECT_NAME}}_BaseArchitecture::AC5000_Platform
        //     //   {{PROJECT_NAME}}_BaseArchitecture::IEC_61131_3
        //
        // supersedes : (none, this is the initial baseline)
    }
}

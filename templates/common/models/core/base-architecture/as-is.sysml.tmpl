// As-Is Base Architecture (mandated subset).
//
// Generated by /vse-setup brownfield as-is survey on {{DATE}}.
//
// Each part def below was classified as MANDATED by the user during
// the survey, meaning it is externally constrained by parent
// organisation, customer, parent product, or regulator (methodology
// §2.1 corollary 1). Constraints are immutable per §2.6 rule 2.
//
// This is acknowledgement work captured under §2.7 Discovery. No
// stakeholders, concerns, or stories were synthesised to justify
// these decisions (§2.6 rule 7). See
// docs/as-is-classification.md for evidence and source-of-mandate
// per element.
//
// Promoting an element from contingent to mandated, or retiring a
// mandated element, is a change to a baselined artefact and requires
// a Change Request per the project's .iso-config.yaml.

library package <{{PROJECT_SHORT_CODE}}_BA> {{PROJECT_NAME}}_BaseArchitecture {

    private import VSE_Library::ConfigItem;
    private import VSE_Library::CIState;

    // {{AS_IS_MANDATED_PART_DEFS}}
    //
    // Per mandated row from the as-is survey, /vse-setup emits one
    // part def in this body, with the following shape:
    //
    //     part def <ElementName> {
    //         @ConfigItem {
    //             ciId = "BA-AS-IS-<NN>";
    //             baselineId = "BL-BA-AS-IS-0.1";
    //             ciState = CIState::Baselined;
    //             owner = "{{AUTHOR}}";
    //         }
    //         attribute version : String = "<declared-version>";
    //         attribute mandateSource : String = "<one of: parent-organisation, customer, parent-product, regulator, other>";
    //     }
    //
    // No `require constraint` clauses are emitted at survey time.
    // The user adds them later via @architecture-design once the
    // engineering implications of each given are understood.
}
